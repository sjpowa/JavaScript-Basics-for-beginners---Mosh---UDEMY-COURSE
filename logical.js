// && and ||

// if ur car has 4 wheel and it is red,
// write in the console TRUE otherwise false
let wheels = 4;
let color = 'blue';

let car = (wheels == 4 && color == 'red') ? true : false
console.log(car)