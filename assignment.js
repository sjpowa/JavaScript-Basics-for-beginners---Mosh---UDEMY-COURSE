// Assignment Operators
let x = 10;

// now if we have to add like 5 euro to the first 10, we have to do like this

// x += 5; // so if we declare the operation before the = we add the x value with 5, so we have 10 + 5
console.log(x);

// x -= 3;
console.log(x);

// x *= 5;
console.log(x);

x /= 5;
console.log(x);

